const config = require('../../config.json')

module.exports = {
    name: "create character",
    sintax:config.prefix+"create character <name>",
    args:true,//comando ultiliza argumentos (sim ou não)
    argsQtd:1,
    execute(message,Args){
        
    },
}