const config = require('../../config.json')

module.exports = {
    name:"music next",
    description:"skip's music",
    sintax:config.prefix+"music next",
    args:false,//comando ultiliza argumentos (sim ou não)
    execute:function(message,Args){
    }
}